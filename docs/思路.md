1 解释渲染流程逻辑，
2 渲染系统应注册到内置系统中(包括现有所有成品系统都应该在内置系统自动注册)，这样示例场景将无需关心, 但是这样会有gameworld耦合太多以及editorController无法控制渲染流程的问题，所以1不动GameWorld而是实现其子类来获得定制化GameWorld，2将update与render分离这样外部可以分别控制解决editorController无法控制渲染流程问题
3 渲染层级与ysort应该为切换关系而不是串行关系，
4 渲染层级重设计