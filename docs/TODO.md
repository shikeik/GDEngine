ignored: indentFix codeFormatFix

TODO   








bug修复与功能优化改善(不改善严重影响使用才算): 
undo不起效问题，

多个屏幕共用父类getViewport实例问题，

bloom的相机问题，
- (已修复)现在Bloom捕获再渲染有个相机不一致的bug: 原因应该是当游戏相机移动后会捕获当前视窗内图像然后结束后渲染这时其实做了两次cam.projection变换导致图像捕获与显示屏幕都发生偏移

编辑器快捷工具按钮会有多个亮起的问题






增加内容: 
DebugUI
- 这个调试视图我觉得可以更新了, 因为现在在编辑器界面就不需要log视图了, 所以我觉得可以做一个在屏幕上贴边的按钮来切换显示隐藏(现在是k键, 但是手机上不显示虚拟键盘就没有这个键(我们应逐渐脱离对虚拟键盘的依赖))

BioNode: 
- 现在只有固定动画，希望增加多动画, 甚至动画状态机(当然状态机的逻辑按理不应该在内部而是外部控制，内部只应记录所有动画表数据)
- 虽然有BioPartType.PREFAB类型，但现在BioRenderer还不能绘制PREFAB：考虑实现是直接加载json为prefab还是通过将其在构建时从json转化为自身子节点实例的方式

可视化编辑器
- (已解决)Star.fill有bug: 有一条凹角被画成一条边了
- 顶点增删: 
- - 现在是inspector按钮，不方便，增加左上快捷工具顶点编辑模式，
- 增加时间速率调节(有时我不希望显示动画时我希望速率为0，或者我要更仔细查看动画轨迹时我希望速度降得很低)
- console现在只能显示log, info则显示不了, 我希望可以同时显示log与info, 也可以切换单独显示一种, 这个可能要设计下

主游戏: 
- 攻击瞄准功能: 手机端拖动瞄准没问题, 但是电脑端就很鸡肋, 所以可以根据userType如果为pc则采用以鼠标按下点为目标的瞄准形式(绘制上也要同步)
h5的连击特效字体还没有还原
小怪头顶的小血条也没有还原: 被攻击时显示, 血量无变化1s后消失，血量变化有过渡动画







定制滑动布局: 
- (已完成)必须点在它本身才可触发触屏滑动，
- 必须鼠标悬浮于布局内才可以触发滚轮滚动
定制数值输入栏: 
- (已完成)数值文本居中对齐
- (已完成)在旁增加小按钮<>，按下并拖动来模拟滑条更改值的功能(加上这个按钮后输入栏宽度应与同宽度没加的相同以此保持对齐美观性)


(已完成)然后禁用取消选中功能, 老是误触就取消了


相机控制: 
- (已完成)现在有个致命的问题: 滚动布局会抢相机缩放的焦点因为用到滚轮被滚动布局拦住了, 我的解决思路是: 做一个判定鼠标在不在滚动布局范围内悬浮, 不在那就取消焦点即可


可视化编辑器
- (已解决)Star.fill有bug: 有一条凹角被画成一条边了
- (已完成但效果不好待返工)现在世界视图没有网格, 并且我希望处于0, 0的那两条要更粗一些
- (已完成)需要绘制一条紫线来从选中节点连接到父系0,0点(根节点则连到世界0,0点)以展示节点的偏移
- (已完成)现在只有旋转的操作手柄, 还需要拉伸的手柄
- (已完成)需要增加在世界视图点击选中节点(可以根据原点的距离选中最近的节点, 但可能有一些选中争议复杂性, 可能需要实际体验来最终确定)
- (已完成)需要增加类似unity的f快速聚焦到选中节点的功能(包括位移与缩放(缩放需要刚好能看全节点并留一定空间的大小)), 手机上可以增加一排快捷栏按钮于世界视图左上方
- (已完成)现在可视化编辑器一点都不可视化编辑, 因为全是滑条操作, 应该参考基础形状编辑器的操作, 逐渐转为视图上的直接操作编辑
- 现在由于在手机上开发迭代了多个版本, 电脑的适配落下了, 后面得补上
- (已完成, 右/中键移动)电脑上鼠标移动不了相机, 没有做适配好像




多数场景现在没有分世界与ui视口的区别, 得遍历更新所有这些场景

现在生物定制几乎都不统一: 具体表现在：1.颜色不能统一乘上传入颜色，2.有些生物甚至没有缩放适配

生物部件掉落需要生物是由多个部件组成才可拆分，但现在绝大部分生物都不是基于BioNode组合的，为了统一，之后有时间最好是将所有代码生物外形全部转为BioNode生物外形


操作优化: 还原h5的操作体验(但额外增加摇杆移动)
- (已完成)这里摇杆采用独特设计: 固定悬浮位置，但拖动超出半径则摇杆中心会被拖动超出半径的距离









未来:  
解析现有蓝图系统的不足，以及思考修复不足后蓝图的扩展和上限的可能性(它的上限按理应该很广很高)
 
挑战模式与大世界模式：
现有模式作为挑战模式，无尽波次流程循环
之后加入无尽随机大世界模式：
  在大世界攻略巢穴，击败，掉落，组装，提升
  然后实力足够继续挑战模式，大世界，挑战...交替往复..








已搞定  

-[x] 把平台接口方法统一放到ScreenManager的初始化输入处理器来监听并分发, 平台本身只需实现接口, 用户层上什么也不用做  
-[x] 波次管理器：可以做类吸血鬼幸存者那样的波次(或者初设计时更简化)

四功能(已完成): 
- 网格绘制(0,0点需要更粗, 同样恒定视觉大小)，
- 双指防误触(0.3s后还是单指再确定为选中操作)，
- ctrlz(手机做成左上角功能键), 
- 将移动拉伸位移变为左上角功能键切换使用不容易误操作: 
- - (并且gizmos也只显示当前操作的手柄)










已搁置  

将以前的Debug适配到当前项目, 包括log与info输出, 以及ui显示优化(最简单的方案就是输出到现在的ui上, 但输入方法全部改为以前的Debug)  

版本日志: 
- 想在游戏内加个更新日志板块.比如右上角一个按钮点击打开更新日志窗口, 上tab版本号列表, 下content版本内容.