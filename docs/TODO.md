ignored: indentFix codeFormatFix  

## 版本

### `1.8.14.x`
- [x] 增加引擎版本信息, 用于显示模板所属引擎版本, 以及对用户项目进行libs更新(用户可自行选择是否更新, 对比引擎当前版本与项目版本在右上角提示有/无更新)

### `1.8.13.x`
- [x] 没有详细信息
- [x] 用户可以上传项目到模板目录或已有则覆盖升级版本: 需要填写新版本号并通过模板合规审查: 包括编译审查和一些其他审查.
  - 老师我们现在更新有点硬更的感觉, 实际上应该模仿同类引擎的方式,   
    楚他们到底有没有这种行为, 但是我觉得可以让用户写的项目注入到内置模板项目,   
    提供一个申请加入项目模板的功能, 输入版本号, 申请, 只需要像内置注入assets/那样注入,   
    虽然得改点东西, 而且我们得加检测, 如果没有就直接复制, 如果有就"更新版本", 并且要设置审查, 包括编译审查和其他审查

### `1.8.12.x`
- [x] 可以选择模板创建
- [x] 将硬编码模板升级为真实完善的模板结构
- [x] 为了应对以后可能很多脚本项目模板的情况, 以及脚本项目可能会更新的情况:
  - 创建一个gradle复制任务来把Projects/所有模板复制过去,
      - 但是这里有个问题, 有可能会掺杂一些其他人分享的脚本项目, 所以得区分内置模板项目和用户项目,
      - 我们只复制内置模板项目, 用户脚本项目不管:
          - 内置模板项目路径改为: GDEngine/InternalProjectTemplates/
          - 用户改为: GDEngine/UserProjects/
          - 但以上只是我们在开发时用于复制脚本辨认的, 实际上运行时脚本开发者不会有这个GDEngine/InternalProjectTemplates/,
              - 但是引擎开发者会从这里运行项目, 所以还是需要在hub显示这里的项目
      - 总之有了复制任务我们就可以方便的开发, 更新项目模板, 也就是"官方游戏与教程"
  - 第二个, assets/路径也要整改, 添加assets/engine/来表示所有引擎的东西, 然后里面才是libs/和script_project_templates/

### `1.8.11.x`
- hub右键删除项目(弹确认窗口)
- editor的文件加个右键菜单: 如果是IGameScriptEntry则显示设为启动脚本
- 新建项目自动不重名
- 继续平台分辨率差异适配
- DebugConsole快捷键Z展开收起
- 双击打开项目

### `1.8.10.x`
- 增加引擎级配置: 配置脚本项目根目录(这一是为了用户可以自行决定项目根位置, 二主要是为了让我能够用git将手机端与电脑端的Projects/数据同步并直接引用(所以为了实现这个还需要个平台参数, 安卓就在安卓的项目路径/Projects/下电脑就在电脑路径的Projects/下))

## 待办

静态内部类的独立化，大类封装清理，多余场景清理


一个实现起来比较模糊的功能：让脚本项目有使用引擎外外置jar的能力

项目内文字全中文化(考虑做i18n多语言)

带文字图标的TextLabel, 看怎么做, 我希望所有用到文字的地方都可以使用格式化字符来在显示时转为小图标.
- 感觉这种图标资源可以直接从平台自身获取, 文字也可以从平台自身获取
- 启动脚本应该带个房子小图标
LogErr得做了: 可以做一个解析格式化前缀文本为颜色字体的东西, 然后LogErr直接"<#FFFFFFFF>文字<#666666>文字2", 这里标准做法是什么我忘了, 写法是这样吗我也忘了

sourceJar得搞了(先得有注释才行, 给所有引擎方法打注释), 可以直接在ide跳转javadoc给脚本开发者写教程, 这必须得做起来  

dialog重设计, 太难看了  

视口配置做成项目配置: 可配置横屏或竖屏, 以及长短边分辨率, 在Editor增加项目设置窗口来配置  

吸纳开发者的好主意:
```
诶！我有个好主意
我把我的引擎做的游戏放taptap上
然后给个说明，这游戏是gd引擎做的，然后跳转qq群(这会同时吸引玩家与开发者, 所以得说明对引擎感兴趣的可加此群)按钮，以此吸纳玩家与开发者
不过要做到这步还需要最后的步骤，引擎得能把自己的运行时和脚本开发者的项目合在一起打包成apk和exe
```

Editor的New Script上下文菜单还在用硬编码脚本模板, 这里需要和创建项目的统一下逻辑, 新脚本文件模板也改为从assets/script_project_templates/NewScript.java获取的方式
脚本项目结构的规范化进阶
现在世界相机的视口大小和ui视口缩放绑定的, 按理应该基于基准视口大小而不是和ui视口耦合, 有空改






## bug修复与功能优化改善(不改善严重影响使用才算): 






# 增加内容: 







## 已完成: 
- [x] 限制变换组件为等比缩放: 把方法改为只能设置1个值xy相等即可








## 已搁置
做ui布局编辑器: 可调整控件和布局的对齐方式，调整布局位置...
- 使用xml做配置数据, 就像android可视化布局开发体验一样, 很舒服

