## 版本

## `1.10.x` 对标UnityEditor整合GDEngineEditor
### `1.10.6.x`
- [ ] 修复渲染层级
### `1.10.5.x`
- [x] 优化变换手柄操作体验
- [x] 相机右键拖拽移动不跟手
- [x] pc滚轮缩放, android双指缩放拖拽移动不起效的问题
### `1.10.4.x` 
- 正式转为引擎开发
- 清理所有旧代码, 包括SoloEditor, IconEditor, GDEngine业务代码
### `1.10.3.x` 真正的TransformComponent-使用全套Affine2矩阵变换
- [x] 完成功能开发, 对接所有旧代码适配
### `1.10.2.x` 解决AddCompDialog只显示用户组件，内置没了的问题
### `1.10.1.x` 升级Inspector面板, 实现扫描式Add Component按钮与搜索式DialogMenu
### `1.10.0.x`
- [x] 增加类加载器的自动更新
- [x] 修复快捷键映射报错

## `1.9.x` 图标编辑器
### `1.9.9.x` EcsEditor
- [x] 完成通用编辑器对Ecs系统的适配
### `1.9.8.x` 
- [x] 移动操作可以沿单轴线移动
- [x] 保存现在有toast以及toolbar按钮
- [x] (临时解决)MRS操作都可以对齐: 网格对齐, 数值对齐0.1/1.0
### `1.9.7.x` 
- [x] 不能保存, 没有文件树(放到hierarchy上面成分割栏): 统一存在local/iconProjects/*.json, 如果目录列表为空, 进入编辑器自动创建1个并标*表示未保存, ctrl+s保存并取消*标记
- [x] 内部类独立化独立到各自文件中去, 以及考虑未来支持GObject以及Ecs仿Unity编辑器的通用化处理
### `1.9.6.x` 体验修复
- [x] 给MRS undo/redo增加快捷键使用方式
- [x] 参数lable会被输入条遮挡: 统一创建所有类型的智能输入
### `1.9.5.x` picking
- [x] 不能在场景点击来切换选中
### `1.9.4.x` undo/redo
- [x] 编辑器中禁用相机wasd移动
- [x] 没有undo/redo, 需要记录所有单条操作, 增加操作历史面板(可显示/收起), toolbar增加undo/redo快捷工具
### `1.9.3.x` 清理优化
  - [x] 清理代码, 以及结构设计与优化
  - [x] 删除时会闪退
### `1.9.0.x` - `1.9.3.x` 开发
- [x] 添加子系改名为添加形状, 并可以添加所有类型(现在只有圆形和矩形)
- [x] 拖拽手柄没有统一在面板右对齐
- [x] 添加操作后hierarchy选中状态没了
- [x] 节点不能改名, 应该右边name处可以改名
- [x] 检查器面板还需要显示物体形状类型, 并可更改形状
- [x] 现在不可调整节点排列顺序
- [x] 了解颜色被重置原因并修复bug
- [x] 顺序调整和父子级调整操作没有虚拟gizmo反馈, 容易误操作, 顺序调整应该增加虚拟边线表示, 父子级调整应该增加线框表示
- [x] 添加物体时如果同级有重名自动迭代+1 
- [x] 没有json视图, 给inspector变为两个tab页, 第二页是json视图, 里面显示BioCodeEditor类型的文本编辑器, 并有应用按钮在修改后可以应用到场景

### `1.8.14.x`
- [x] 增加引擎版本信息, 用于显示模板所属引擎版本, 以及对用户项目进行libs更新(用户可自行选择是否更新, 对比引擎当前版本与项目版本在右上角提示有/无更新)

### `1.8.13.x`
- [x] 没有详细信息
- [x] 用户可以上传项目到模板目录或已有则覆盖升级版本: 需要填写新版本号并通过模板合规审查: 包括编译审查和一些其他审查.
  - 老师我们现在更新有点硬更的感觉, 实际上应该模仿同类引擎的方式,   
    楚他们到底有没有这种行为, 但是我觉得可以让用户写的项目注入到内置模板项目,   
    提供一个申请加入项目模板的功能, 输入版本号, 申请, 只需要像内置注入assets/那样注入,   
    虽然得改点东西, 而且我们得加检测, 如果没有就直接复制, 如果有就"更新版本", 并且要设置审查, 包括编译审查和其他审查

### `1.8.12.x`
- [x] 可以选择模板创建
- [x] 将硬编码模板升级为真实完善的模板结构
- [x] 为了应对以后可能很多脚本项目模板的情况, 以及脚本项目可能会更新的情况:
  - 创建一个gradle复制任务来把Projects/所有模板复制过去,
      - 但是这里有个问题, 有可能会掺杂一些其他人分享的脚本项目, 所以得区分内置模板项目和用户项目,
      - 我们只复制内置模板项目, 用户脚本项目不管:
          - 内置模板项目路径改为: GDEngine/InternalProjectTemplates/
          - 用户改为: GDEngine/UserProjects/
          - 但以上只是我们在开发时用于复制脚本辨认的, 实际上运行时脚本开发者不会有这个GDEngine/InternalProjectTemplates/,
              - 但是引擎开发者会从这里运行项目, 所以还是需要在hub显示这里的项目
      - 总之有了复制任务我们就可以方便的开发, 更新项目模板, 也就是"官方游戏与教程"
  - 第二个, assets/路径也要整改, 添加assets/engine/来表示所有引擎的东西, 然后里面才是libs/和script_project_templates/

### `1.8.11.x`
- hub右键删除项目(弹确认窗口)
- editor的文件加个右键菜单: 如果是IGameScriptEntry则显示设为启动脚本
- 新建项目自动不重名
- 继续平台分辨率差异适配
- DebugConsole快捷键Z展开收起
- 双击打开项目

### `1.8.10.x`
- 增加引擎级配置: 配置脚本项目根目录(这一是为了用户可以自行决定项目根位置, 二主要是为了让我能够用git将手机端与电脑端的Projects/数据同步并直接引用(所以为了实现这个还需要个平台参数, 安卓就在安卓的项目路径/Projects/下电脑就在电脑路径的Projects/下))
